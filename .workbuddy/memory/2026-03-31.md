# Working Memory - 2026-03-31

## ZY管理系统 v2.0 升级完成

**时间**：2026-03-31 10:30
**状态**：代码已推送 GitHub，Railway 自动部署中

### 本次更新内容

#### 1. 系统重命名
- 系统名称从"Excel数据管理系统"改为"ZY管理系统"
- 所有页面标题、Logo、登录页已更新

#### 2. 新增物资申报模块（月度）
- **管理员功能**：
  - 上传物资清单 Excel（物品名、单价、规格）
  - 查看所有申领记录
  - 审批申领（通过/拒绝）
  - 删除申领记录
  - 按部门统计额度使用情况

- **普通用户功能**：
  - 下拉选择物资，自动匹配规格单价
  - 输入数量，自动计算总金额
  - 实时显示部门剩余额度
  - 查看自己和同部门人员的申领记录
  - 额度不足时自动拦截

#### 3. 新增部门管理模块
- 创建/编辑/删除部门
- 设置部门月度额度
- 实时统计各部门使用情况

#### 4. 用户管理升级
- 用户分部门归属
- 批量导入用户（Excel）
- 批量删除用户
- 下载导入模板
- 编辑用户部门和角色

#### 5. 权限控制
- 普通用户只能查看本部门申领
- 部门额度实时校验
- 超额自动拦截

### 技术实现
- 新增数据文件：materials.json, applications.json, departments.json
- 新增路由：materials.js, departments.js
- 新增工具模块：utils/materials.js
- 前端页面完全重构，新增物资申报和部门管理界面

### 部署状态
- GitHub 仓库已更新
- Railway 自动部署中（约2-3分钟）

### 部署问题修复
**问题1**：Railway部署失败  
**原因**：`routes/materials.js` 第20行上传路径指向了不存在的 `uploads` 目录（正确应为 `data/uploads`）  
**修复**：已修正路径配置，提交 commit c9f9a4b

**问题2**：Route.get() requires a callback function but got a [object Undefined]  
**原因**：`routes/materials.js` 导入 `requireAuth`，但 `middleware.js` 导出的是 `requireLogin`  
**修复**：统一使用 `requireLogin`，提交 commit e1f605a

### v2.1 功能升级（commit 27400f0）
**新增功能**：
1. **审批流程完善**：用户申领后状态为 pending，管理员可通过/驳回
2. **申领管理模块**：管理员专用页面，可查看/筛选/编辑/导出所有申领数据
3. **广播公告功能**：物资申领页面顶部显示广播，管理员可编辑公告内容
4. **手动添加物资**：管理员可直接手动添加物资，无需上传Excel
5. **物资管理**：管理员可编辑/删除已添加的物资

**API新增**：
- `GET/POST /api/materials/broadcast` - 广播管理
- `POST /api/materials/material` - 手动添加物资
- `PUT/DELETE /api/materials/material/:id` - 编辑/删除物资

**前端新增**：
- 申领管理导航菜单（仅管理员可见）
- 广播公告卡片（支持多行文本）
- 物资管理弹窗（添加/编辑/删除）
- 申领记录导出CSV功能

### 使用流程
1. 管理员先创建部门并设置额度
2. 管理员上传物资清单 Excel
3. 管理员分配用户到部门
4. 普通用户登录后申领物资
5. 管理员审批申领

---

## v2.2 功能升级

**时间**：2026-03-31 11:30

### 新增功能：管理员重置用户密码
- **后端API**：`PUT /api/users/:id/password` - 管理员重置指定用户密码
- **前端界面**：用户管理页面增加"重置密码"按钮，点击后弹出密码重置对话框
- **安全限制**：管理员不能通过此接口重置自己的密码（需使用"修改密码"功能）
- **操作日志**：重置密码操作会被记录到操作日志中

---

## 数据备份说明

**时间**：2026-03-31 12:36

### 备份文件
- **位置**：`data_backup_20260331.zip`
- **内容**：包含 data/ 目录下所有文件
  - `users.json` - 用户数据
  - `logs/` - 登录日志和操作日志
  - `sessions/` - 会话文件
  - `uploads/` - 上传的Excel文件
  - `.secret` - Session密钥

### Railway 数据恢复步骤
1. 重新部署后，进入 Railway Dashboard → 你的服务 → Shell
2. 执行以下命令：
   ```bash
   cd /app
   # 上传 data_backup_20260331.zip 到项目根目录
   unzip data_backup_20260331.zip -d /tmp/backup
   cp -r /tmp/backup/* data/
   ```
3. 重启服务

### 注意事项
- Railway 每次重新部署会丢失容器内数据
- 建议配置 Railway Volume 或使用外部数据库实现持久化
- 免费版 Volume 限制 0.5GB

---

## v3.0 重大功能升级

**时间**：2026-03-31 13:30

### 1. 修复用户管理模板功能
- **模板下载**：修复 `/api/users/template` 接口，添加目录创建和错误处理
- **模板上传识别**：增强列名匹配，支持多种中文/英文列名变体

### 2. 新增审批流程模块
- **审批流程配置（管理员）**：
  - 创建/编辑/删除审批流程
  - 支持多种流程类型：通用、请假、报销、采购、加班、出差
  - 多步骤审批，每步可指定多个审批人
  - 启用/停用流程控制
- **审批中心（所有用户）**：
  - 发起申请：选择流程类型，填写标题和内容
  - 待我审批：查看并处理待审批的申请
  - 我的申请：查看申请状态、撤回待审批申请
  - 申请详情：查看审批流程进度和每步审批意见
- **数据存储**：`data/workflows.json` + `data/workflow_instances.json`

### 3. 首页全新改版
- **欢迎区**：显示用户欢迎语和当前日期
- **天气组件**：自动获取当前城市天气（基于IP）
- **统计卡片**：Excel文件数、系统用户数、待我审批、我的申请
- **人力行政告示**：管理员可编辑，支持多行公告
- **今日热点新闻**：内置新闻列表展示
- **快捷操作**：快速跳转常用功能
- **最近文件**：显示最近上传的5个文件
- **我的待办**：聚合待审批和申请中的事项

### 新增文件
- `utils/workflow.js` - 审批流程数据管理
- `routes/workflow.js` - 审批流程API路由

### 修改文件
- `server.js` - 注册新路由
- `routes/users.js` - 修复模板下载和上传识别
- `public/index.html` - 全新首页UI + 审批模块UI

---

## v3.1 物资申领功能升级

**时间**：2026-03-31 14:00

### 1. 物资申领开关控制（管理员）
- **后端API**：
  - `GET /api/materials/settings` - 获取申领开关状态
  - `POST /api/materials/settings` - 设置申领开关（管理员）
- **前端界面**：
  - 物资申报页面显示开关按钮（仅管理员可见）
  - 开启状态：🔓 申领开启（绿色按钮）
  - 关闭状态：🔒 申领关闭（红色按钮）
  - 点击切换需确认，操作记录到日志
- **用户端表现**：
  - 关闭期间，普通用户访问物资申报页面看到锁定提示
  - 关闭期间，提交申领API返回403错误"物资申领功能已关闭"
  - 管理员仍可正常查看和管理

### 2. 申领人字段
- **数据模型**：`applications.json` 新增 `applicantName` 字段
- **前端表单**：
  - 备注栏上方添加必填项"申领人"
  - 文本输入框，用户可自行填写
  - 标记为必填项（红色*）
- **后端验证**：提交时检查申领人不能为空
- **记录展示**：
  - 物资申报页面申领记录显示申领人
  - 申领管理页面显示申领人（与申请人账号分开）

### 修改文件
- `utils/materials.js` - 新增开关设置数据管理
- `routes/materials.js` - 新增开关API和申领人字段处理
- `public/index.html` - 新增开关UI和申领人表单字段

### 部署问题修复
**问题**：部署后返回502错误  
**原因**：`routes/workflow.js` 导入 `requireAuth`，但 `middleware.js` 导出的是 `requireLogin`  
**修复**：统一将所有 `requireAuth` 替换为 `requireLogin`，提交 commit 982aa2a  
**状态**：✅ 已修复，部署成功（HTTP 200）

---

## v3.2 UI优化

**时间**：2026-03-31 14:10

### 1. 登录界面改版（字节跳动风格）
- **设计风格**：极简主义、大量留白、黑白主色调
- **布局**：左右分栏，左侧登录表单，右侧视觉展示
- **配色**：纯白背景 + 黑色按钮 + 灰色辅助文字
- **字体**：大标题 48px，简洁有力的视觉层次
- **交互**：输入框hover/focus状态、按钮悬停动效
- **响应式**：移动端自动隐藏右侧视觉区

### 2. 人力行政告示版式优化
- **卡片式布局**：每条公告独立卡片展示
- **图标装饰**：每条公告配有不同emoji图标
- **智能解析**：支持"标题：内容"格式自动分割
- **视觉层次**：标题加粗 + 内容灰色 + 底部元信息

### 3. 新闻功能完善
- **详情内容**：每条新闻添加完整的正文内容
- **点击交互**：点击新闻标题弹出详情弹窗
- **详情展示**：标题 + 来源/时间元信息 + 正文段落
- **样式优化**：新闻项悬停效果、热门标签样式

### 部署状态
- ✅ 代码已推送到 GitHub (commit 74d7ed1)
- ✅ Railway 自动部署完成
- ✅ 应用访问正常 (HTTP 200)

---

## v3.3 UI全面现代化改造

**时间**：2026-03-31 14:30

### 改造内容

#### 1. 设计系统升级
- **主色调**：科技感蓝紫渐变 (`#6366f1` → `#8b5cf6`)
- **阴影系统**：5级阴影层级（xs/sm/md/lg/xl）+ 发光效果
- **圆角系统**：6级圆角（xs/sm/md/lg/xl/full）
- **动画系统**：3级过渡速度 + 弹跳动画
- **字体**：Inter 字体家族（Google Fonts）

#### 2. 全局过渡动画
- 所有交互元素添加平滑过渡
- 页面切换动画
- 悬停状态微交互
- 模态框进入/退出动画

#### 3. 侧边栏现代化
- 浅色主题（替代深色）
- 图标+文字布局优化
- 激活状态发光效果
- 悬停缩放动画
- 宽度增加至260px

#### 4. 卡片组件升级
- 更柔和的阴影
- 更大的圆角（16px）
- 悬停提升效果
- 渐变顶部边框（统计卡片）

#### 5. 按钮系统重构
- 统一按钮样式（primary/secondary/danger/success/ghost）
- 悬停上浮效果
- 渐变背景（主按钮）
- 发光阴影

#### 6. 表格组件升级
- 表头样式优化（大写+字间距）
- 行悬停效果
- 单元格编辑状态样式
- 圆角表格容器

#### 7. 表单组件升级
- 输入框聚焦发光效果
- 标签样式统一
- 表单验证样式
- 搜索栏样式优化

#### 8. 模态框升级
- 背景模糊效果（backdrop-filter）
- 进入/退出缩放动画
- 更大的圆角
- 更柔和的阴影

#### 9. 加载状态优化
- Skeleton 加载动画
- 旋转加载图标
- 空状态插画

#### 10. 新增组件
- **开关组件**：滑动开关样式
- **工具提示**：hover提示
- **步骤条**：流程展示
- **时间线**：审批流程展示
- **分页器**：表格分页
- **标签页**：内容切换

### 文件变更
- **新增**：`public/modern-styles.css`（完整现代化CSS，约800行）
- **修改**：`public/index.html`（引入外部CSS和Inter字体）
- **备份**：`public/index.html.backup`（原始文件备份）

### 技术实现
- CSS变量系统（60+个变量）
- 响应式设计（桌面/平板/手机）
- 打印样式支持
- 无障碍焦点样式
- 滚动条美化

### 部署状态
- ✅ 本地验证通过
- ⏳ 待推送到GitHub

---

## v3.4 物资申领体验优化

**时间**：2026-03-31 14:35

### 1. 提交体验优化
- **按钮状态反馈**：提交时按钮显示"提交中..."并禁用，避免用户困惑
- **自动清空表单**：提交成功后自动清空所有表单字段，方便继续申领下一项
- **错误处理**：提交失败时显示友好提示

### 2. 用户撤销申领功能
- **功能说明**：普通用户现在可以撤销自己未审批（pending状态）的申领申请
- **UI位置**：在物资申报页面的申领记录列表，备注列显示"撤销"按钮
- **权限控制**：
  - 只能撤销自己的申请
  - 只能撤销未审批（pending）状态的申请
  - 已审批或已拒绝的申请无法撤销
- **后端API**：`DELETE /api/materials/my-applications/:id`
- **额度返还**：撤销后部门额度自动返还

### 修改文件
- `public/index.html` - 优化submitApplication函数，添加deleteMyApplication函数，修改申领记录列表UI
- `routes/materials.js` - 新增 `DELETE /my-applications/:id` 路由

### 部署状态
- ✅ 本地验证通过
